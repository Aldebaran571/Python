#creando tuplas con tuple
#tupla = tuple(["john", "nieto"])

#creando una tupla sin parentesis con multiples datos
tupla = "john", "nieto"

#creando una tupla sin parentesis con un solo dato

tupla = "john",#se agrega la (coma)al final para que diferencie una coma de un string

print(tupla)